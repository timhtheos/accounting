{% extends 'base.html.twig' %}

{% set subheading   = "Account type" %}
{% set breadcrumbs  = "Edit" %}

{% block body %}
  <div class="pull-right">
    <a href="/account-types" class="btn btn-default">Cancel</a>
    <a href="/account-type/delete/{{ account_type.id }}" class="btn btn-default">Delete</a>
  </div>

  <h2 class="page-header">Edit account type `{{ account_type.name }}`</h2>

  {{ form_start(form) }}
  {{ form_widget(form) }}
  {{ form_end(form) }}
{% endblock %}
