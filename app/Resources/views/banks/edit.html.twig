{% extends 'base.html.twig' %}

{% set subheading   = "Bank" %}
{% set breadcrumbs  = "Edit" %}

{% block body %}
  <div class="pull-right">
    <a href="/banks" class="btn btn-default">Cancel</a>
    <a href="/bank/delete/{{ bank.id }}" class="btn btn-default">Delete</a>
  </div>

  <h2 class="page-header">Edit bank `{{ bank.name }}`</h2>

  {{ form_start(form) }}
  {{ form_widget(form) }}
  {{ form_end(form) }}
{% endblock %}
